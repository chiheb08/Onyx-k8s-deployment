apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: onyx-api-server
  namespace: onyx  # Change to your Onyx namespace
  labels:
    app: onyx-api-server
spec:
  selector:
    matchLabels:
      app: onyx-api-server
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s

