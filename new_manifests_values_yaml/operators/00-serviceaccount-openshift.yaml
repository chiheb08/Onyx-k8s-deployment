# ServiceAccount for operator-managed Postgres/OpenSearch pods on OpenShift.
# After apply, cluster admin may need:
#   oc adm policy add-scc-to-user nonroot -z onyx-data-plane -n onyx-infra
apiVersion: v1
kind: ServiceAccount
metadata:
  name: onyx-data-plane
  namespace: onyx-infra
